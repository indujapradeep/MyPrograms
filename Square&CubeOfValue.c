#include<stdio.h>

int main()
{
	int a,b;
	printf("Enter a value to find the square of a number:");
	scanf("%d",&a);
	printf("Square of the %d is %d",a,a*a);
	printf("\nEnter a value to find the cube of a number:");
	scanf("%d",&b);
	printf("Cube of %d is %d",b,b*b*b);
}
